#if !defined(__BORLANDC__)
namespace M302de {
#endif

/* 0x20 */
void FIG_do_enemy_action(struct enemy_sheet*, signed short);

/* 0x25 */
void FIG_use_item(struct struct_hero*, struct enemy_sheet*, struct struct_hero*, signed short, signed short);

#if !defined(__BORLANDC__)
}
#endif
